'use strict';

/**
 * descripcion-carnet service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::descripcion-carnet.descripcion-carnet');
